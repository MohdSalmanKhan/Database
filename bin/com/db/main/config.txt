BEGIN
Idaentificatioan_newwwd
name, varchar(50)
street, varchar(50)
city, varchar(50)
state, varchar(50)
zip, float
END
BEGIN
Idaentificatioan_newwwd
name, varchar(50)
street, varchar(50)
city, varchar(50)
state, varchar(50)
zip, float
END
BEGIN
Idaentificatioan_newwwd
name, varchar(50)
street, varchar(50)
city, varchar(50)
state, varchar(50)
zip, float
END
